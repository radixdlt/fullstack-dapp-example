{{- if .Values.metrics.enabled }}
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "radquest-admin.serviceAccountName" . }}
  labels:
    release: prometheus-operator
    {{- include "radquest-admin.labels" . | nindent 4 }}
spec:
  endpoints:
  - port: metrics
  namespaceSelector:
    matchNames:
    - {{ .Release.Namespace | quote }}
  selector:
    matchLabels:
      {{- include "radquest-admin.selectorLabels" . | nindent 6 }}
{{- end }}
