{{- if .Values.metrics.enabled }}
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "radquest-dapp.serviceAccountName" . }}
  labels:
    release: prometheus-operator
    {{- include "radquest-dapp.labels" . | nindent 4 }}
spec:
  endpoints:
  - port: metrics
  namespaceSelector:
    matchNames:
    - {{ .Release.Namespace | quote }}
  selector:
    matchLabels:
      {{- include "radquest-dapp.selectorLabels" . | nindent 6 }}
{{- end }}
