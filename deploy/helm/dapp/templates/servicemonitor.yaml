{{- if .Values.metrics.enabled }}
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "radquest-dapp.serviceAccountName" . }}-metrics
  labels:
    release: prometheus-operator
    {{- include "radquest-dapp.labels" . | nindent 4 }}
spec:
  endpoints:
  - port: metrics
  namespaceSelector:
    matchNames:
    - {{ .Release.Namespace | quote }}
  selector:
    matchLabels:
      {{- include "radquest-dapp.selectorLabels" . | nindent 6 }}
{{- end }}
{{- if .Values.pm2_metrics.enabled }}
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "radquest-dapp.serviceAccountName" . }}-pm2-metrics
  labels:
    release: prometheus-operator
    {{- include "radquest-dapp.labels" . | nindent 4 }}
spec:
  endpoints:
  - port: pm2-metrics
  namespaceSelector:
    matchNames:
    - {{ .Release.Namespace | quote }}
  selector:
    matchLabels:
      {{- include "radquest-dapp.selectorLabels" . | nindent 6 }}
{{- end }}
