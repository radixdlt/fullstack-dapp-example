{{- if .Values.metrics.enabled }}
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "radquest-letty-swap.serviceAccountName" . }}
  labels:
    release: prometheus-operator
    {{- include "radquest-letty-swap.labels" . | nindent 4 }}
spec:
  endpoints:
  - port: metrics
  namespaceSelector:
    matchNames:
    - {{ .Release.Namespace | quote }}
  selector:
    matchLabels:
      {{- include "radquest-letty-swap.selectorLabels" . | nindent 6 }}
{{- end }}
